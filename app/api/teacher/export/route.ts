import {db,user,json,readWorld} from '@/lib/world';
export async function GET(req:Request){if(await user(req)!=='teacher')return json({error:'Teacher sign-in required.'},403);const {w}=await readWorld();if(w.battle)return json({error:'Finish the current wave before exporting.'},409);const accounts=await db().prepare('SELECT id,nickname,password,salt,created FROM accounts').all();return Response.json({format:'manor-quest-backup-v1',exportedAt:new Date().toISOString(),world:w,accounts:accounts.results},{headers:{'Cache-Control':'no-store','Content-Disposition':'attachment; filename="manor-quest-private-backup.json"'}})}
